import styled from 'styled-components';

const Header = () => {
    return (
        <AppTitle>
            Pomodoro
        </AppTitle>
    )
}

const AppTitle = styled.h1`
text-align: center;
`

export default Header
